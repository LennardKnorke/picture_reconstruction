Given an input image and a picture folder (or keyword), the programm will reconstruct the input image using the image folder or images it finds on the internet using the keyword

to do:
  - construct everything as a function/ class
  - folder choice/ keyword input
  - webscraping for images as the keyword.
  - optimize speed and resolution choice!
